<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
    <entry key="selectEmpAll">
        select
        *
        from
        employee
    </entry>

    <entry key="insertEmployee">
        insert into employee (
        emp_id,
        emp_name,
        emp_no,
        email,
        phone,
        dept_code,
        job_code,
        sal_level,
        salary,
        bonus,
        manager_id,
        hire_date,
        ent_date,
        ent_yn
        )
        values(?,?,?,?,?,?,?,?,?,?,?,?,?,?)

    </entry>
</properties>